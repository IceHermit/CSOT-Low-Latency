# Week 1 Project — Fastest Correct Strategy

> **Mission:** Build a minimal C++ trading-platform runner that executes a **judge-defined strategy spec**, replays historical market ticks against it, verifies correctness, measures end-to-end latency per decision, and prints a summary. Over the next 4 weeks we will turn this into a multi-threaded, lock-free, networked, leaderboard-ranked beast. This week, just get the correct synchronous baseline walking.

## Output Preview

<img width="1092" height="192" alt="image" src="https://github.com/user-attachments/assets/b24e4fe7-86cb-4114-87cc-43beabe843b6" />

## Directory Layout

```
project/
├── CMakeLists.txt             
├── .gitignore                 
├── README.md                  ← You are reading this
├── build.sh                   ← A quick bash script that compiles your strategy code
├── include/
│   ├── strategy.hpp           
│   ├── histogram.hpp         
│   └── engine.hpp             
├── src/
│   ├── engine.cpp             
│   └── main.cpp               
├── strategies/
│   └── spec_strategy.cpp      ← A re-implementation of the same strategy as the sample one
│   └── sample_strategy.cpp    ← The sample strategy (copied from the original repository)
└── data/
    ├── gen.py                 ← generates random-data of some size
    ├── tiny.csv               
    ├── synthetic_small.csv   
    └── synthetic_large.csv    ← gitignored, generated by gen.py
```
## Build Instructions
- Write your strategy code or use one of the strategies provided.</li>
- Run `./build.sh <input_file> <output_file> <include_directory>` on your terminal. This tells the build.sh script to compile the input file while including everything in the include directory, and save the object code in the specified output file.<br>Example: `./build.sh strategies/sample_strategy.cpp strategies/sample_strategy.so ./include`
- You now have your .so file. You can run it with main.cpp (compile main.cpp first). Say your compiled main.cpp file is called quant_runner, then you would run `./quant_runner <strategy.so> <ticks.csv>` where `<ticks.csv>` is the csv file with the data on which the strategy is to be run

## Hardware
The hardware on which this program was run on has the following specifications:
- Processor: Intel(R) Core(TM) i5-7200U CPU @ 2.50GHz   2.70 GHz
- RAM: 8.00 GB
- Graphics Card: Intel(R) HD Graphics 620 (128 MB)
- System Type:	64-bit operating system, x64-based processor
- OS: Windows 10

## Optimizations
- The first optimization to be made was to replace the for-loops that calculate the sum every tick with rolling-sums which store the sum of the previous window. These sums are updated by adding the new value and subtracting the oldest value. This gives a major boost as it an O(1) operation per tick.
- Then, the unordered_map was replaced with a simple linear search for symbol-matching. This works better as accessing an item from a vector/array is much faster than from a map in C++
- The calculation of standard deviation was removed entirely, as the sqrt function takes up a lot of cpu cycles. Consequently every value that the standard deviation was being compared to was now squared and then compared with the variance instead. A similar trick was used to avoid division. At many places, division by x replaced with multiplication by (1/x) as multiplication is inherently faster in low-level operations

## Structure of Input Data
`tiny.csv`,`synthetic_small.csv`, and `synthetic_large.csv`, all use the following structure which is mentioned in the reference repository
```
timestamp_ns | symbol | bid_px | ask_px | bid_qty | ask_qty
```

## Latency Numbers
According to the online judge, the latency numbers achieved by `spec_strategy.so` are:
- p50 = 24ns
- p99 = 50ns
- p999 = 93ns
- throughput = 18.08 M/s

## `perf stat` Snippet
<img width="1158" height="366" alt="image" src="https://github.com/user-attachments/assets/a89a61c7-0a65-45ac-a91c-c2472b53ed90" />

## Interesting observations
